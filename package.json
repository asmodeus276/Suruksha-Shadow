{
  "name": "suraksha-shadow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm --prefix client run build && node server/index.js",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "start": "node server/index.js",
    "build": "npm --prefix client run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.9",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "multer": "^2.3.0",
    "vite": "^8.1.1"
  }
}
